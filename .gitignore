# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Claude Code local session artifacts (worktrees, local settings) — not
# project source, never meant to be committed.
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
lint_results.txt
.env
.env.*
*.pem
*.key
GITHUB_SSH_KEY.txt
*.tar.gz
*.zip
*_output.txt
*lint*.txt
uploads/
node_modules/
full-backup-*.json

# Prisma generated client (ignored so it doesn't clutter diffs)
src/generated/client/

# Workspace and lockfiles
pnpm-lock.yaml
pnpm-workspace.yaml

# Temporary query engine files
*.tmp*
*.dll.node

# Test files
dbb-test.js

# pub files
sshadithyan
sshadithyan.pubfirebase-service-account.json

# Firebase Credentials
firebase-service-account.json